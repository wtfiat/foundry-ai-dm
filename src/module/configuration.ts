// This project is a Foundry module, not a full game system.
// The starter template includes configuration augmentation examples for systems.
// We keep this file as an empty module so the path remains stable without tripping lint rules.
export {};
